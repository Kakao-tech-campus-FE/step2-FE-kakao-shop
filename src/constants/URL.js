const HOME = '/';
const LOGIN = '/login';
const SIGNUP = '/signup';
const PRODUCT = '/product/:id';
const CART = '/cart';
const ORDER = '/order';
const ERROR = '/error';

const URL = { HOME, LOGIN, SIGNUP, PRODUCT, CART, ORDER, ERROR };

export default URL;
