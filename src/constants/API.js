const AUTH = {
  LOGIN: "/login",
  CHECK: "/check",
  JOIN: "/join",
};

const PRODUCT = {
  PRODUCTS: "/products",
};

Object.freeze(AUTH);
Object.freeze(PRODUCT);
export default { AUTH, PRODUCT };
