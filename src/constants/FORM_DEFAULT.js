const LOGIN = {
    email: '',
    password: '',
};

const SIGNUP = {
    username: '',
    email: '',
    password: '',
    passwordCheck: '',
};

Object.freeze(LOGIN);
Object.freeze(SIGNUP);

export default { LOGIN, SIGNUP };
