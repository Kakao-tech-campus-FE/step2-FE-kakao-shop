const LOGIN = Object.freeze({
    email: '',
    password: '',
});

const SIGNUP = Object.freeze({
    username: '',
    email: '',
    password: '',
    passwordCheck: '',
});

export default { LOGIN, SIGNUP };
