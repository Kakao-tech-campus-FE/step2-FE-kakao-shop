import React from "react";
import { useLocation } from "react-router-dom";

export default function UrlRoute({children}) {
  const location = useLocation();

  if (location.) {
  }
  return <div></div>;
}
