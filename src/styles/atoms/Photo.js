import styled from 'styled-components';

export const ImageContainer = styled.picture`
    .card {
        width: 19vw;
        height: 25vh;
        object-fit: cover;
        border-radius: 10px;
    }
    img {
        width: inherit;
    }
`