import Link from 'react-router-dom';

import React from 'react';

const Card = (to, children) => {
    return (
        <div>
            {children}
        </div>
    );
};
//이번주 과제!
export default Card;