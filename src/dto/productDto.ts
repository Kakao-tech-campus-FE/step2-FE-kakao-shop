export interface productInfo {
  id: number;
  description: string;
  image: string;
  price: number;
  productName: string;
}

export interface OptionInfo {
  id: number;
  optionName: string;
  price: number;
}
