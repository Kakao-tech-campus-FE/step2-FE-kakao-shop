const Input = ({ type, value, name, onChange, placeholder, id }) => {
  return;
  <>
    <input
      id={id}
      type={type}
      value={value}
      name={name}
      onChange={onChange}
      placeholder={placeholder}
    />
  </>;
};

export default Input;
